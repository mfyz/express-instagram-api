extends layout

block body
	h3 My IG Photos

	div.card-columns(style='column-count:4')
		each photo in photos
			.card.mb-3.mr-2
				a(href= photo.link)
					img.card-img-top(src= photo.images.low_resolution.url)